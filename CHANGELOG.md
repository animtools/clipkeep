# CHANGELOG — ClipKeep

新しい版を上に追加します。

## v0.2.0-beta — 2026-09-19

### 追加

- **インスペクタを別のウィンドウに切り離せるようになりました。** 位置と大きさを覚えていて、次に起動したときも切り離したまま同じ場所に開きます。ワークスペースにも保存されます。トレイにしまうと一緒に隠れ、戻すと元どおり切り離された状態で戻ります。
- **作ったあとの Keep を編集できるようになりました。** 範囲・アンカー・プレーヤーで拡大回転した見た目を、**E** キーか右クリックの **「編集…」** から開いて直せます。
- **キーボード操作を整理しました。** **?** でショートカットの一覧を出せます。キーは「いま操作している場所」にだけ効きます。
- **スキャン実験室のショット表が、観ながら整理する画面になりました。** 縦にドラッグして複数行を選び、選んだ範囲だけを再生・ループできます。隣り合う 2 つを選べば境目をまたいで確認できます。ダブルクリックでその範囲を再生、右クリックで操作を選べます。音量も調整できます。
- **シーンの切れ目でタイルを作り直せるようになりました**（スキャン実験室）。
- **並びが整っているタイルを作り直すとき、サムネイルだけを撮り直すようになりました**（範囲は保たれます）。

### 変更

- **シーン検出が約 1.9 倍速くなりました。** 12.3 分の動画で 133 秒 → 70 秒（結果は以前と完全に同じです）。
- **検出の進み具合がフレーム単位で出るようになりました。** どの方式で読み取っているかも、進捗と完了の行に出ます。
- **タイルと Keep の範囲の数え方を統一しました。既存のライブラリは初回起動時に一度だけ自動で移し替えます。** 見た目や範囲は変わりません。
- **カットフレームの手動編集と、シーンの切れ目からのタイル作り直しが、サブスク（All-Access）限定になりました。** メインウィンドウからカットフレームの編集はできなくなり、スキャン実験室に集約しています。
- インスペクタの **「タイル」** タブを開いたとき、最初からタイルの一覧が出るようになりました。

### 修正

- 再生位置から Keep を作ったりクリップを編集したとき、画面に映っているフレームとずれることがあったのを直しました。
- Keep のアンカーを動かしたあと、カードの絵が古いまま残ることがあったのを直しました。
- タイルを作り直すときの確認画面で、何がいくつ失われるのかを種類ごとに数えて出すようにしました。
- 切り離したウィンドウでのホバープレビュー・右クリックメニュー・ショートカットの取りこぼしを直しました。
- スキャン実験室で、再生に合わせて選択が勝手に書き換わるのをやめ、行の強調だけにしました。
- ショット表が、統計をまだ作っていない動画で空になるのを直しました。

## v0.1.1-beta — 2026-09-15

### 変更

- **ポータブル版は、フォルダの外にある ffprobe / ffmpeg を使わなくなりました。** 以前のインストール版などが残した外部ツールを拾っていたため、フォルダごと別の PC へ移すと取り込みが動かないことがありました。初回設定の「外部ツール」から取得すると、exe と同じフォルダの `ClipKeepTools` に入ります。
- ライセンス管理の案内から、このベータ版で使えない「外部 AI 連携」の記載を外しました。

### 修正

- 縦長の動画でタイルを複数選ぶと、クリップのサムネイルが横長に切り取られていたのを直しました。
- アーカイブを開く前に動画をアーカイブすると、アーカイブの一覧にその回に移した動画しか並ばず、件数と合わなかったのを直しました。
- アーカイブ中の動画のカードに、タイルがあっても「タイルなし」と出ていたのを直しました。
- 「すべて」と「アーカイブ」を切り替えたあと、カードの「タイルなし」の表示が切り替える前の状態のまま残ることがあったのを直しました。

## v0.1.0-beta — 2026-09-09

最初の公開版です。

- 動画フォルダを取り込むと、フレームから作ったタイルで中身を一覧できます
- タイルにマウスを乗せると、その場でプレビューが再生されます
- 気になった範囲を Keep タイルとして集められます。クリップエディタで範囲を調整できます
- タグ・評価・検索で絞り込めます
- Keep から短いアニメーション（ダイジェスト）を作れます
- 有料プランで書き出し、All-Access（サブスク）でスキャン実験室（顔・ポーズ・シーン検出・文字起こし）が使えます
- 無料で使えるのは動画 30 本・Keep タイル 100 枚までです。上限を超えてもデータは消えません
